import * as actions from './actions'; // eslint-disable-line no-unused-vars

const initialState = {};

export default function exchangeOrderReducer(state = initialState, action = {}) {
  switch (action.type) {
    default:
      return state;
  }
}
